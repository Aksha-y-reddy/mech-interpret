# Minimal requirements for Colab - core packages only
# Many packages are already pre-installed in Colab

# Fine-tuning (if not already installed)
peft>=0.8.0
trl>=0.7.0
bitsandbytes>=0.42.0
accelerate>=0.26.0

# Mechanistic Interpretability
transformer-lens>=1.15.0
einops>=0.7.0
fancy-einsum>=0.0.3

# Fairness & Bias Detection (lightweight)
fairlearn>=0.10.0

# Experiment Tracking
wandb>=0.16.0

# Utilities
sentencepiece>=0.1.99
rouge-score>=0.1.2

# Statistical Analysis (if needed)
scipy>=1.11.0

